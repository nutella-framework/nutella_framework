# RoomCast Main App web interface
Framework interface to log into nutella and play RoomCast's channels.

To build:
```
npm install -g gulp     # Optional, if you don't have gulp
npm install             # To install dependencies
gulp                    # To build
```