require_relative '../command'

class Add < Command
  def run(args=nil)
    puts "Running command 'add' - NOT YET IMPLEMENTED"
    # Copies an arbitrary template (from file or URL) into the current project
  end
end
